{% extends '::master.html.twig' %}
{% block content %}
<div class="well">
    <div class="container-fluid" style="text-align: center">
        <div class="row">
            <div class="col-md-8">
                Titre

            </div>
            <div class="col-md-4">
                Actions
            </div>
        </div>
        {% for formulaire in formulaires %}
            <div class="row" >
                <div class="col-md-8" >
                    {{ formulaire.title }}
                </div>
                <div class="col-md-2">
                    <a href="{{ path('formulaire_show', {'idForm': formulaire.id}) }}"> <button  class = "btn-primary" >
                        Show
                    </button></a>
                </div>
            </div>
        {% endfor %}

    </div>
</div>
{% endblock %}